Minimize the scope of local variables

- Minimize the scope of local variables by declaring them where they are first used.

- Nearly every local variable should contain an initializer, the exception being try/catch.